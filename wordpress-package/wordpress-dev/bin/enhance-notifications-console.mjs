import { readFile, writeFile } from 'node:fs/promises';

const sourcePath = '/home/ubuntu/keleva-audit-tools/runtime/keleva-native-console.html';
let source = await readFile(sourcePath, 'utf8');

if (!source.includes('keleva-notifications-console-v1')) {
  const css = `/* keleva-notifications-console-v1 */.mk-notification-list{list-style:none;margin:14px 0 0;padding:0}.mk-notification{display:grid;grid-template-columns:12px minmax(0,1fr);gap:10px;padding:13px 0;border-top:1px solid var(--line)}.mk-notification:first-child{border-top:0}.mk-notification-dot{width:9px;height:9px;margin-top:5px;border-radius:50%;background:var(--accent)}.mk-notification strong{display:block;font-size:13px}.mk-notification p{margin:3px 0 0;color:var(--muted);font-size:12px;line-height:1.4}.mk-notification time{display:block;margin-top:4px;color:var(--subtle);font-size:11px;font-weight:750}`;
  const script = `<script>/* keleva-notifications-console-v1 */(()=>{const base='/wp-json/keleva-dashboard/v1',q=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function notificationApi(){const r=await fetch(base+'/audit',{credentials:'same-origin',headers:{Accept:'application/json'}}),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||'Les notifications ne sont pas disponibles.');return d}function eventLabel(event){return String(event||'Mise à jour').replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase())}function renderNotifications(events){q('#mk-panel').innerHTML='<div class="mk-panel"><div class="mk-panel-head"><div><div class="mk-kicker">Notifications</div><h2>Ce qui demande votre attention</h2></div><button id="mk-close-notifications" class="mk-secondary">Fermer</button></div><div class="mk-panel-body"><div class="mk-config-header"><span><b>Votre journal clair.</b> Les changements de la boutique apparaissent ici : commandes, catalogue, apparence et actions de sécurité.</span></div><section class="mk-section"><h3>Activité récente</h3><p>Ces informations viennent du journal d’audit de la boutique. Elles ne contiennent ni mot de passe ni clé d’intégration.</p>'+(events.length?'<ul class="mk-notification-list">'+events.map(e=>'<li class="mk-notification"><span class="mk-notification-dot" aria-hidden="true"></span><div><strong>'+esc(eventLabel(e.event))+'</strong><p>'+('order_status_updated'===e.event?'Le statut d’une commande a été actualisé.':'Une action a été enregistrée pour votre boutique.')+'</p><time datetime="'+esc(e.at||'')+'">'+esc(new Date(e.at).toLocaleString('fr-FR',{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'}))+'</time></div></li>').join('')+'</ul>':'<div class="mk-empty">Aucune notification pour le moment. Les prochaines actions apparaîtront ici.</div>')+'</section></div></div>';q('#mk-close-notifications').onclick=()=>q('#mk-panel').innerHTML=''}async function openNotifications(){try{const d=await notificationApi();renderNotifications((d.events||[]).slice(0,30))}catch(e){const error=q('#mk-error');if(error)error.textContent=e.message}}function addNotificationsEntry(){const nav=q('.mk-nav');if(nav&&!q('#mk-notifications-entry')){const b=document.createElement('button');b.id='mk-notifications-entry';b.className='mk-ghost';b.type='button';b.textContent='Notifications';b.style.color='#fff';b.style.fontSize='12px';b.onclick=openNotifications;nav.appendChild(b)}}addNotificationsEntry()})();</script>`;
  source = source.replace('</style>', `${css}</style>`);
  source = source.replace('<!-- /wp:html -->', `${script}\n<!-- /wp:html -->`);
  await writeFile(sourcePath, source, 'utf8');
}

console.log('Patch notifications console : OK');
